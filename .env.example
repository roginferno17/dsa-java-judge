# Local dev server port (used by JavaJudge.bat and `npm run dev`)
PORT=3000

# Judge limits. These are defaults; the Settings page overrides them per run.
EXECUTION_TIMEOUT=5000
EXECUTION_MEMORY_LIMIT=256
